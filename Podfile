# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'movies_inc' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for movies_inc
   pod 'Alamofire', '~> 5.1'
   pod 'RxSwift', '~> 5.1.1'
   pod 'RxCocoa', '~> 5.1.1'
   pod 'Kingfisher'
   pod 'Cosmos', '~> 18.0'
  target 'movies_incTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
